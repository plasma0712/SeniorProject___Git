using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ETC_ : MonoBehaviour
{

	// Use this for initialization
	void Start () {
        Buffer.Instance.CharacteristicReset();
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
